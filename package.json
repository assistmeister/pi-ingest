{
  "name": "pi-ingest",
  "version": "0.1.0",
  "description": "Cheap context ingestion for oh-my-pi: deterministic workspace scan + planner-ready briefing. Run under a cheap model (/model @smol), then hand the briefing to your planner.",
  "type": "module",
  "pi": {
    "extensions": ["./src/ingest.ts"]
  },
  "omp": {
    "extensions": ["./src/ingest.ts"]
  },
  "scripts": {
    "typecheck": "bunx --bun tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "0.73.1",
    "bun-types": "^1.4.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "typebox": "^1.1.24"
  }
}
